n = int(input("Введите число n: "))
# преобразуем. В строку
# Для того чтобы число принялось как строка
send = str(n)
s1 = send + send
s2 = send + send + send
comp = n + int(t1) + int(t2)
print("Результат равен:", comp)